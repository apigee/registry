#!/bin/sh

cli flame update-project --project.name projects/atlas --project.display_name "Atlas" --project.description "from the APIs.guru OpenAPI Directory"

cli flame update-project --project.name projects/google --project.display_name "Google APIs" --project.description "from the Google API Discovery Service"
